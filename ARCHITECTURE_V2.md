# GMS Ver2.17.1 アーキテクチャ

## 収支表XLSX

- `balance-allocation.ts` は保存済み月次の採用報酬を日別配分する。時給は勤務時間比、売上報酬は本指名＋場内延長の日別売上比、バック・給与分美容室は発生日へ計上し、最終出勤日に月額との端数を調整する。
- 体入即日美容室経費を給与へ再加算しない。スタッフの勤務分・ドライバー日給は当日へ、月額手当・端数は本人の最終出勤日へ計上する。保存月額は再計算しない。
- `balance-export.ts` は承認済み日次のみを検証。POS勤務・指名/同伴明細から店舗全体（派遣含む）の本数を取得し、不足・不一致は推定せず出力を停止する。
- 見本へ紹介料列を追加。P列は派遣キャスト、R列はスタッフ・ドライバー・派遣スタッフ、S列は紹介者支払総額、T列は日次経費・派遣手数料・固定費・納品酒代・カード手数料。
- 下段は列追加に伴い総従業員給がW36、総支出がU37、キャスト差引支給/準備金がU41/U44になる。カード入金はExcel内のJ42/O42へ入力し、M38を再計算する。GMSには入金額を保存しない。
- 月額の紹介料・固定費・納品酒代・カード手数料は、対象月の最後の承認済み営業日へ自動計上する。承認操作順・未承認の日付・暦の月末は使用しない。承認済み営業日がない場合は、計上できない理由を表示して出力を停止する（費用もない空月は出力可）。
- M38の従業員給与はドライバーを含む日払い控除後金額とし、日払い・派遣支払・源泉税は各1回控除。展開した控除式と帳票の簡約式を検証して二重控除を防ぐ。

## 実行環境

- Next.js 16 / React 19 / TypeScript
- Firebase Authentication（メールアドレス＋パスワード）
- Firebase Realtime Database
- Vercel（`main` は本番、`dev` はプレビュー）

FirebaseプロジェクトはGMS専用の `club-genesis-gms` を使用する。

Firebaseの保存ルートはホスト名で切り替える。本番の
`club-genesis-accounting.vercel.app` だけが `accounting`、それ以外は
`accounting-dev` を使用する。

## 権限

- `shop`: 店舗作業、共通フォーム
- `accounting`: 経理作業、共通フォーム
- `op`: 店舗作業、経理作業、共通フォーム

権限はFirebase Realtime Databaseの `users/{uid}/role` で一元管理する。
Realtime Databaseルールでも同じ値を検査し、古い権限が残らないようにする。

## 主要データ

- `casts`: 在籍・体入・退店キャスト、月度時給、紹介者、顧問料
- `staff`: 在籍・体入・退店スタッフ、月度時給
- `drivers`: 在籍・退店ドライバー、日給
- `introducers`: 紹介者と報酬条件
- `liquorCosts`: シャンパン・ワイン／キープボトル原価
- `config/cashFloat`: つり銭（初期値200,000円）
- `history`: POS原本、店舗入力、計算結果、承認履歴を含む日次スナップショット
- `history/{id}/submittedAtMs`: Firebaseサーバーが確定した店舗送信順
- `posSubmissionClaims`: POSチェックサムを安全な64桁hexキーとして使い、同じPOS JSONの重複登録を防ぐ所有情報
- `dailyClosingDeletionLock`: 未承認日次の削除と月次確定・状態更新を排他する一時ロック
- `introducerEntryEvents`: 入店顧問料を採用月に一度だけ計上する履歴
- `introducerMonthEvents`: 紹介者削除・同月再設定を人物単位で保持する月次履歴
- `introducerDeletionCommits`: 紹介者削除と対象キャストを証明する変更不能の履歴
- `introducerDeletionLocks`: 紹介者削除中のキャスト変更を防ぐ一時ロック
- `accountingAdjustments`: 月次の源泉所得税・各種手当・固定経費

マスターを完全削除しても `history` 内の名称・時給・原価・金額のスナップショットは変更しない。
送迎ドライバーの日払いは `history` の日次勤務へ保存し、当日現金残額から控除する。
経理側では月次の総支給額から日払い合計を控除して差引支給額を算出する。
経理側のキャスト売上画面は、承認済み `history` をキャスト・出勤日単位に展開し、
本指名／場内延長の売上と酒代原価、指名・同伴本数、各種バック、ボトル、美容室手当を表示する。
キャスト売上の「全員分をXLSX出力」は、選択月の全員をキャスト別シートへ出力する。
月次確定済みの月は保存された売上・報酬スナップショットを使い、現行マスターから再計算しない。
添付キャストデータ表に合わせ、G/J/M列は本指名/場内指名/同伴バック、
P/Q列は通常ボトル/シャンパン・ワインバック、U列はドリンクバックとする。
通常ボトル・シャンパン等の種類別バックは日別・月合計の `bottleBackByKind` に保存する。
旧確定データで内訳が未保存の場合はP/Q列を結合し、保存済みの合計額であることを明記する。
下部には時給＋バックと売上報酬の両方を比較表示し、採用方式と実際の差引支給額を別記する。
売上報酬率は保存された実際の率を表示し、日払い・その他は日払い＋立替＋送迎代の合計とする。
店舗側のキャスト商品明細は、ドリンクをPOS販売単価ごとにまとめて杯数を表示する。
対象外を含むPOS全件商品表は店舗画面には重複表示せず、経理承認画面で確認できる。

キャスト別の本指名売上・場内延長売上、月間時給報酬、各バック、売上報酬基礎、
売上報酬は10円未満を切り捨てる。体入キャストの即日支払いは営業日ごとに
10円未満を切り捨てる。ボトル・ドリンクの％バックは商品1行全体の金額を
10円未満切捨て後、対象人数で均等割りし、各人の金額も10円未満切り捨てる
（1,000円を3人で分配する場合は各330円、合計990円。35,000円・原価12,500円の
シャンパンを2人で分配する場合は、商品全体5,620円、各2,810円）。美容室手当、日払い・立替・
送迎代、源泉所得税および差引支給額は追加で丸めず1円単位を保持する。
POSの現金売上・カード売上・当日合計売上は照合対象の実額であり、丸めない。

## 日次状態

`submitted`（店舗送信）→ `approved`（経理承認）または `returned`（差戻し）。
`approved` からも `returned` へ戻せる。差戻し時は実行者・直前状態・理由を記録し、
店舗で再編集・再送してから再承認する。`returned` の期間は月次集計から除外する。
店舗は承認前または差戻し後のデータを `withdrawn` にして再編集できる。
`submitted`・`returned`・`withdrawn` は店舗またはOPが完全削除できる。`approved` は直接削除できず、
経理またはOPによる差戻しが先に必要となる。削除時は `history` 本体、対応する
`posSubmissionClaims`（旧形式キーも含む）、削除ロックを一括更新し、復元不能な物理削除とする。
対象月が月次確定中または確定済みの場合は削除できない。

紹介者報酬は営業日順ではなく、月内で最後に店舗保存された日次の紹介者条件
（紹介者なしを含む）を同月の全日次へ遡って適用する。新規保存は
`submittedAtMs` をFirebaseサーバー時刻で記録する。旧データの保存順を復元できない場合、
または同一サーバー時刻に異なる条件が競合する場合は、推測せず月次確定を停止する。
紹介者削除・同月再設定は日次とは別の原子的な月次履歴で管理し、削除月は報酬なし、
同月に再設定した場合は再設定後の条件を月全体へ適用する。入店顧問料の履歴は
後月のマスター変更では書き換えず、採用日そのものを変更した場合だけ未確定月内で補正する。

Realtime Databaseが空配列を省略して保存するため、日次・POS原本・月次調整は
読込境界で配列と必須オブジェクトを正規化する。構造欠損は画面上で警告する。

## 経費表XLSX

経費画面から、選択月の承認済み日次と保存済み経理入力を `GENESIS経費表_YYYY-MM.xlsx` へ出力する。
見本の列配置・罫線・游ゴシック・A4横向き印刷を使用し、同日同科目の複数支払先は改行で併記する。
各名目欄は支払先だけを表示し、金額は隣の金額欄に集計する。
人員や固定費が見本の行数を超える場合は必要な行を追加して全件を保持する。
未承認・差戻し中・店舗編集中の日次は含めない。

- 広告宣伝①：紹介料。広告宣伝②：広告等。
- 交通費：交通費・その他（交通費に関連するその他を含む）。その他：派遣手数料。
- 美容室：体入キャストの当日支払い分である `beautyTrial` 経費のみ。
- 派遣支払：派遣キャスト支払と派遣スタッフ支払の合計。派遣手数料は重複させない。
- 女子総支給額：採用された報酬方式ごとに、美容室手当を含む控除前の総支給額を集計する。
- スカウト報酬：紹介者IDごとに合計し、紹介者名と合計金額を表示する。キャスト別には展開しない。
- 酒代納品書分：月締め調整後の金額を固定費へ一度だけ計上する。

月次確定後は報酬・給与・紹介者支払に保存済みの確定結果を使用する。
旧確定結果で紹介者IDがない場合は、保存済みの既知IDと一致する情報から復元できる場合だけ集計し、
紹介者を特定できない場合は推測せず出力を止める。
日次のID・チェックサム・更新日時と経理入力の保存世代が確定時の参照情報と一致しない場合は、
推測して補完せず出力を止める。明細・科目別月計・給与等の総支給額・総支出を照合し、
帳票の総支出が経理の収支データと一致することを検証する。
