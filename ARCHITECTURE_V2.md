# GMS Ver2.27.0 アーキテクチャ

## 店舗送信済み日次の印刷プレビュー（Ver2.27.0、devのみ反映）

- 送信済み一覧の「プレビュー」は、表示時点の日次を用紙形式のモーダルへ表示する。「印刷・PDF保存」はブラウザーの印刷機能を使い、PDF生成サービスや新しい保存先へ業務データを送らない。
- 既存の `DailyPreview` を再利用し、キャスト・商品・勤務・経費・現金照合の保存済み値と不明な旧情報の表示を維持する。④送信確認や経理画面・計算式・POS取込仕様は変更しない。
- A4横、複数ページ、表ヘッダー再掲・長文折返しに対応する。印刷用ルールは専用プレビューへ限定し、背景画面と操作ボタンを印刷しない。元の入力状態を保持し、閉じる／Escapeでタイトル・スクロール制限・フォーカスを戻す。
- devだけにcommit/pushし、main・Firebase Rules・POS・保存済み業務データには変更を加えない。

## 既存営業日の新規重複取込・送信の案内（Ver2.26.2、devのみ反映）

- 保存済み日次がある営業日のJSONを新規作成フォームへ取り込んだ場合は、入力値を変更する前に止める。差戻し・取下げは既存日次から再編集、確認待ちは取下げ後に再編集、承認済みは経理・OPの差戻し後に再編集するよう営業日付きで案内する。
- 新規下書き復元や画面表示中に同営業日データが追加された場合も、店舗画面に既存日次の案内と送信済み一覧へのリンクを表示し、照合確定・送信を停止する。入力中の金額・照合・現金入力や既存日次は自動削除・結合・上書きしない。
- サーバー保存直前にも同じ状態別案内を行い、再編集元の更新日時を自動補完しない。再編集元が消えていた場合は新規作成として復活させず停止する。更新日時照合、承認状態、月次ロック、現金改訂・POS重複防止は維持する。
- 今回もdevのみcommit/push。本番・main・Firebase Rules・POS・保存済み業務データの変更は行わない。

## LAN HTTP開発環境のチェックサム・ID生成（Ver2.26.1、devのみ反映）

- LANのHTTP接続では `crypto.subtle` と `crypto.randomUUID` が公開されないため、ブラウザー互換処理を `src/lib/crypto-compat.ts` に集約する。WebCryptoがある場合は従来の処理を使い、ない場合だけ固定依存の `@noble/hashes` で同じUTF-8バイト列のSHA-256を計算する。
- POSチェックサムと月次データ照合はそれぞれの既存JSON正規化を維持する。チェックサムの省略・不一致の受入れ・計算仕様の変更・確定結果の書換えは行わない。WebCryptoが存在して処理が失敗した場合はエラーを隠さない。
- 取込後の店舗入力・保存・マスタ登録・各ロックも、HTTPで利用可能な `crypto.getRandomValues` によるUUID v4へフォールバックする。`Math.random` や日時は使わず、安全な乱数がない場合は明示的に停止する。ID形式・権限・Firebase接続先・認証・Rulesは変更しない。
- 今回はdevブランチのみcommit/pushし、mainへのマージ・本番デプロイは行わない。POS側や保存済み業務データは変更しない。

## スタッフ月度時給（Ver2.25.0）

- 在籍スタッフ・アルバイトの単価を `hourlyRates[YYYY-MM]` で管理し、未設定月は直近の過去月を継承する。未確定月は対象月全体に適用し、日ごとに1円未満を切り捨てる。体入日は保存済み体入単価、2026年8月以前は保存済み勤務単価を保持する。
- 既存スタッフの現在登録時給を2026年9月度（将来採用者は採用月）の初期時給として読み取る。初めて月度設定を保存する際に初期月も永続化する。互換用の `hourlyRate` は過去日のために維持し、月度変更で上書きしない。マスタ・日次・確定結果の一括移行は行わない。
- 共通フォームで対象月を指定して登録・変更する。採用月以前・導入月以前・0円以下は拒否し、過去の月度キーを削除しない。店舗の新規勤務は営業月の時給を参照する。保存済み勤務の記録単価・日払い・現金照合は自動変更せず、給与との差額を月次の差引支給額へ反映する。
- 新しい月次結果は `staffPayroll.hourlySources` に営業日・勤務元ID・区分・時間・採用単価を保存する。給与画面の日別内訳と収支表の日別配分はこの基準を使う。Ver2.25.0以降の確定結果は日別金額・月額と検算し、確定後のマスタ変更／削除から独立させる。旧確定結果は当時の集計・帳票基準を保持し、現在の単価を後付けしない。
- 勤務時刻と15分単位の勤務時間が一致しない記録、適用単価が0円・不正な記録は名前・ID・営業日付きで警告し、保存／月次確定を停止する。新規未送信の在籍スタッフを勤務欄へ再追加した際にも入力済み日払いを保持する。

## キャスト報酬の受領書・明細書XLSX（Ver2.26.0）

- キャスト報酬画面から全員分を1つのXLSXへキャスト別シートで出力する。未確定月は承認済みの結果、確定月は保存済みスナップショットを使い、報酬額を再計算しない。未保存・確定処理中・データ不整合・確定結果欠損時は停止する。
- 採用方式に合わせた受領書を左側、添付の時給／売上報酬明細書を右側に配置する。現在の受領書と同じカスタム用紙281・横向き・余白・各プリンターバイナリを保ち、横1×縦1ページへ収める。全員印刷はExcelの「ブック全体を印刷」を使用する。
- `public/templates/cast-receipt-v3.xlsx` に2組の空様式を収録。左右で異なる行高は双方の境界を使って行を細分化し、縦結合で各欄の物理寸法を保つ。右側はJ:T列。時給用A1:T46、売上用A1:T45を印刷範囲とする。`receipt-layouts.json` が元テンプレートの論理セルと出力行を対応付け、JSZipで値を記入する。旧v1/v2は既存画面の参照用に保持する。
- 受領書の金額・名前は従来の対応を維持し、受領日・氏名サイン欄は空欄。明細書は年月・源氏名・現在登録済みの本名（未登録は空欄）・勤務日数／時間・実適用時給を記入する。時給方式は基本給と5種類のバック、売上方式は本指名＋場内延長売上・酒代50％・実際の採用率・採用額を使う。両様式の美容室・総支給・源泉・前払金（日払い）・送迎・立替・控除合計・差引支給は同じ結果を使う。手当・減給等の未対応欄は空欄。
- 新たな月次結果には `castRewards.appliedHourlyRates` を保存する。月内の適用単価を重複なく保持し、複数は明細書で併記。旧確定の未保存は「未保存」と表示し、平均時給や現在マスタから推測しない。既存の任意フィールド許容ルールで保存できる。読込時に型・範囲・重複を検証し、Firebaseの配列オブジェクト形式も復元する。報酬計算式・月次計算バージョンは変更しない。
- 数値・小数・文字列の型を保持し、数式風の氏名も文字列として記入する。長い名前・複数時給は欄内で縮小。出力中の二重クリック、テンプレート欠損、取得中の対象月・報酬・本名登録情報の変更を検出し、再試行を案内する。

## iPad向けLAN開発環境（Ver2.23.0）

- `npm run dev:lan` はNext.jsを `0.0.0.0:3000` で起動し、同一LAN内のiPadから接続できるようにする。
- RFC1918のプライベートIPv4アドレスはローカル環境として最新版確認を許可する。
- LAN経由でもFirebaseの接続先は `accounting-dev` とし、本番ルート `accounting` には接続しない。
- Next.jsの開発Originは `GMS_ALLOWED_DEV_ORIGINS` で明示的に制限する。

## 全営業日の補充・返済確認と現金改訂（Ver2.22.0）

- 作成時期では画面・編集可否を分けない。過去分も補充・返済を確認入力して `cash.funding` schema 2へ移せる。確定済み月は確定解除まで変更しない。支払い等の既存入力を自動更新せず、実在高の金額入力も復活させない。
- 未記録の補充3金額・実返済額、前日からの未返済額は `null` と0円を区別する。直前日次に未返済記録が無ければ当時の開始残高を入力する。現在の未返済0円から過去の立替・返済を推定しない。前日の記録がある場合は最新の未返済額を引き継ぎ、保存時と違う前提を比較表示する。
- schema 2は実際に返済した金額を保存する。推奨額は未返済と現金余剰の範囲で別計算し、明示操作で入力する。保存済み実返済を再計算して置き換えない。返済は未返済額・設定つり銭を除く返済可能額以内とし、残高・会社補充と個人補充の合計・当時の記録との一致を検証する。
- 日次の修正保存は対象日と直前日との整合を検証する。未確定の後続に不整合が生じても元の実績は保持し、再確認日を一覧表示する。後続の日次を自動修正しない。承認・月次確定・現行の収支表／経費表出力は未確認や繰越不整合の解消まで停止する。後続に確定済み／確定処理中の月（営業日0の月も含む）があり、現金残額または未返済額が影響する修正は確定解除まで停止する。
- 再送時は変更前の日次全体・現金額・操作者・時刻・理由を `cashRevisions/{dailyId}/{operationUUID}` へ保存し、日次本体と同じ原子的更新で確定する。UUIDを使い同日削除・再作成でも衝突させない。履歴は作成専用で上書き・削除不可。Rulesは旧現金の各金額・旧更新日時を検証し、本体と履歴を相互参照する。旧日次全文JSONはRepositoryが取得値から構築する（Rulesでは全文JSONの再シリアライズ比較はできない）。
- 日次の旧更新日時によるCAS・cash lock・改訂UUIDで競合と片側保存を防ぐ。通信結果が不明な場合は本体と改訂履歴の同一操作を読み直して判定し、不明なまま自動再送しない。承認も同じcash lockを使い、前日確認と承認確定の間に現金を変更させない。
- 保存済みschema 1は閲覧と現金不変の再送を維持し、新規または現金変更はschema 2とする。Ver2.21.1の `legacyCashConfirmed` による未記録再送例外は廃止。Ver2.22未満の確定済み月は当時の集計・帳票を保持し、未記録の過去補充を後付けしない。実データの一括移行や推測した自動入力は行わない。

## 以前の限定対応（Ver2.21.1、現行入力には使用しない）

- 導入前の旧日次（サーバー保存済みで `cash.funding` がない日次）は、再編集だけで新しい現金管理の開始日に変えない。現金金額を変更しない再送では、当時の計算残額・実在高・照合差額をそのまま保存する。後続の旧日次に補充・返済記録を要求しない。
- 店舗画面は「保存済み現金照合を変更せず再送する」確認を今回の操作で求め、`legacyCashConfirmed: true` を日次へ記録する。補充入力と新しい現金残額の一致確認とは区別し、保存済みの確認フラグや退避データから自動確認しない。
- 例外は既存旧日次・同営業日・現金全項目不変・今回の明示確認に限定し、RepositoryとFirebase Rulesの両方で検証する。新規日次のfunding欠落、管理開始済み日次からのfunding削除は引き続き拒否する。ロックtokenを旧新判定に使わず、旧日次を複数回再送しても同じ条件で扱う。
- 旧日次の現金残額に関わる金額が変更される場合は警告して停止し、旧実績を自動修正しない。Ver2.21.0の旧日次再編集時にfundingを強制追加して後続の旧日次を不備扱いしていた経路を修正したもので、既存業務データの一括移行や書換えは行わない。

## 現金不足の補充・個人立替の返済（Ver2.21.0導入時）

以下はschema 1の導入時仕様。開始額の入力、実返済の記録、過去日次の修正・未入力判定は上記Ver2.22.0の統一仕様で置き換える。保存済みのschema 1や確定月の読込互換は維持する。

- 営業終了時の計算上現金残額が設定つり銭を下回る分を、次営業日の開店前不足額とする。会社補充と個人補充を別入力し、合計が不足額と一致するまで送信できない。会社補充は返済不要。会社からの営業当日入金は個人立替の返済原資として別記録する。
- 個人未返済額の初期値は利用者確認済みの0円。返済額は `min(前日未返済額＋個人補充, max(0, 営業現金収支＋会社入金))`。返済後未返済額を翌営業日・翌月へ繰り越す。営業日のない月も前月の未返済額を保持する。
- 売上・給与・経費と補充／返済を混同しない。営業現金収支は従来どおり現金売上－日次支出。返済後現金残額は `設定つり銭＋営業現金収支＋会社入金－個人返済`。開店前の補充は設定つり銭に含まれるため二重加算しない。返済後現金余剰と、損益上の利益は別表示する。
- 新方式の日次は `cash.funding`（schema 1）へ前営業日の参照ID・計算上残額・個人未返済額と当日の補充／返済実績を保存する。現金実在高の金額入力は廃止し、計算上残額との一致確認を必須にする。保存する `actualClosingCash` は確認対象の計算上残額、`difference` は0円。現金額や関連入力の変更後は確認を解除し、画面退避からも確認済み状態を復元しない。
- 導入前の日次には補充や返済の履歴を追加せず、旧実在高・旧照合差額を保持する。新方式開始時の不足額だけを直前日次の計算上残額から求める。承認・差戻し・取下げは現金の物理的移動を取り消さず、繰越残高を保持する。
- 送信・完全削除は環境別の `cashManagementLock` で直列化し、ロック取得後に履歴を再読込する。所有者・token・期限を再検証し、月次確定・つり銭設定変更との競合も禁止する。後続日次が参照済みの現金記録の変更、過去への日次追加・削除は警告して停止する。後続の実績を自動再計算・上書きしない。
- 月次の補充／返済集計は損益とは別枠。対象月の新方式日次に未承認・差戻し・取下げがある場合は現金移動集計と月次確定を止める。月次確定の元データ照合には前月以前の現金繰越依存も含める。新しいschema 3／calculationVersion 2.21.0以降は `results.cashFunding` を必須にし、旧確定月には後付けしない。
- 収支表XLSXは新方式日次のある月に限りG43:P45へ会社補充・個人補充・会社送金・個人返済・期首／期末未返済を表示する。M38へ会社補充＋個人補充＋会社送金－個人返済を反映し、利益列・利益額は変更しない。

## 日別時給の1円計算

- 在籍・体入のキャスト／スタッフとも、営業日ごとの「時給×勤務時間」を1円未満切捨てし、日額を月額へ合計する。同一人物・同営業日の複数勤務は合算後に切り捨てる。キャスト時給は基本500円単位の運用だが、新たな単価入力制限は追加しない。
- 未確定月には新計算を月全体へ適用する。保存済みの日払い・現金照合は書き換えず、時給差額は月次の差引支給額に残す。新規体入の日払い初期額は1円計算、保存済み日次の再編集・再照合では既払額を保持する。
- Ver2.20.0で導入したschema 3では、キャスト・スタッフの `hourlyByDay`（営業日・勤務時間・1円日額）を保存する。読込時は日別合計と月額、日付・勤務時間の整合を検証する。新旧端末の切替中はRulesでschema 2／3の両方の保存を許容する。
- 確定済みschema 1／2は当時の金額・帳票配分を保持し、現行計算へ置き換えない。バック・売上・売上報酬の10円単位、ドライバー日給・各種手当・控除は変更しない。

## 紹介者別の支払一覧

- 紹介者支払画面は、紹介者IDごとの対象キャスト数・支払合計を表示し、各行の「詳細」で保存済みキャスト別明細を開く。対象月変更時は詳細を閉じる。
- 画面と経費XLSXは `groupIntroducerPayments` の人物照合・集計を共有する。同名別IDを統合せず、旧確定月は保存済みの人物IDを厳密照合する。特定不能なら警告と元の明細を表示し、現在マスタや名前から補完しない。計算式・保存形式・確定額は変更しない。

## 旧版クライアントの更新保護

- `app/api/release/route.ts` は公開中のVerとビルド識別子をキャッシュなしで返す。Next.jsの独自fetchに旧deployment指定を付けず、起動・画面復帰・オンライン復帰・表示中60秒間隔で確認する。すべての更新APIは `requireUser` から操作開始前に再確認し、確認不能／Verまたはビルド不一致は書込みを開始しない。
- 本番は `club-genesis-accounting.vercel.app`、開発は `club-genesis-accounting-git-dev-lavorofuk-wqs-projects.vercel.app`、ローカルはlocalhost/loopbackの同じoriginを確認する。古い固定deployment URL等では更新を停止して最新環境URLを案内する。ドメインを変更する場合は `app-release.ts` の定義も更新する。
- Nextビルドの `NEXT_PUBLIC_GMS_BUILD_ID` はVercelのGitコミットSHAから設定する。同じVerで別コミットを公開した場合やロールバックも検知する。バージョン表示とAPIは `version.json` を共有する。
- 旧版検知時はフォームの新規更新操作を停止するが、開始済みの更新・ロック解放は完了させる。処理中・JSON読込中・XLSX出力中は画面更新／復元を禁止する。自動リロードはせず、利用者が更新ボタンを押す。
- 更新ボタンは現在のフォーム入力をsessionStorageへ退避し、書込結果を確認してから再読込する。退避は環境・Firebase UID・タブ別で、パスワードやトークンは含めない。容量不足・壊れた形式・未知schemaは再読込せず入力を保持する。利用者の明示操作で復元し、元のrevisionと照合情報を保持して自動保存／送信はしない。
- 店舗のPOS原本・各照合・原価・手当／日払い等・追加前入力、共通フォームの編集項目、経理の対象月と月次調整を復元する。復元項目をフォームがすべて受け取るまで退避を削除しない。承認の「確認済み」は復元せず、再度内容の確認を求める。
- 初回移行の制限：Ver2.17.2以前を開きっぱなしのタブには検知コードがないため、一度手動の再読込が必要。この機能はFirebase Rulesで旧端末を識別する仕組みではない。導入前クライアントをサーバー側で完全拒否する変更は行わない（直接SDK書込の全経路・null削除等をAPI方式へ移行する別設計が必要）。

## Firebase更新処理

- トランザクションはすべて `ready-transaction.ts` の `runReadyTransaction` を経由する。`onValue` の初期通知を待ち、更新が終わるまで監視を保持する。単発の `get()` だけでは保持されないキャッシュを使って、未読込の `null` をデータ不存在と誤判定することを防ぐ。
- 更新関数内の業務エラーは `undefined` を返して安全に中断し、SDKの終了処理後に元のエラーを画面へ返す。真の不存在・不適合な状態・更新版の競合チェックは従来どおり行い、自動で上書き・再試行しない。
- 初期読込は15秒でタイムアウトする。書込開始後は二重処理を防ぐため独自のタイムアウトを設けず、Firebaseの確定／中断を待つ。成功・失敗ともに監視とタイマーを解除する。

## 収支表XLSX

- `balance-allocation.ts` は保存済み月次の採用報酬を日別配分する。未確定月とschema 3の時給は `hourlyByDay` の1円日額を使用する。旧確定schema 1／2は従来の勤務時間比による時給配分と端数調整を保持する。売上報酬は本指名＋場内延長の日別売上比、バック・給与分美容室は発生日へ計上し、最終出勤日に月額との端数を調整する。
- 体入即日美容室経費を給与へ再加算しない。スタッフの勤務分・ドライバー日給は当日へ、月額手当・端数は本人の最終出勤日へ計上する。保存月額は再計算しない。
- `balance-export.ts` は承認済み日次のみを検証。POS勤務・指名/同伴明細から店舗全体（派遣含む）の本数を取得し、不足・不一致は推定せず出力を停止する。
- 見本へ紹介料列を追加。P列は派遣キャスト、R列はスタッフ・ドライバー・派遣スタッフ、S列は紹介者支払総額、T列は日次経費・派遣手数料・固定費・納品酒代・カード手数料。
- 下段は列追加に伴い総従業員給がW36、総支出がU37、キャスト差引支給/準備金がU41/U44になる。カード入金はExcel内のJ42/O42へ入力し、M38を再計算する。GMSには入金額を保存しない。
- 月額の紹介料・固定費・納品酒代・カード手数料は、対象月の最後の承認済み営業日へ自動計上する。承認操作順・未承認の日付・暦の月末は使用しない。承認済み営業日がない場合は、計上できない理由を表示して出力を停止する（費用もない空月は出力可）。
- M38の従業員給与はドライバーを含む日払い控除後金額とし、日払い・派遣支払・源泉税は各1回控除。展開した控除式と帳票の簡約式を検証して二重控除を防ぐ。

## 実行環境

- Next.js 16 / React 19 / TypeScript
- Firebase Authentication（メールアドレス＋パスワード）
- Firebase Realtime Database
- Vercel（`main` は本番、`dev` はプレビュー）

FirebaseプロジェクトはGMS専用の `club-genesis-gms` を使用する。

Firebaseの保存ルートはホスト名で切り替える。本番の
`club-genesis-accounting.vercel.app` だけが `accounting`、それ以外は
`accounting-dev` を使用する。

## 権限

- `shop`: 店舗作業、共通フォーム
- `accounting`: 経理作業、共通フォーム
- `op`: 店舗作業、経理作業、共通フォーム

権限はFirebase Realtime Databaseの `users/{uid}/role` で一元管理する。
Realtime Databaseルールでも同じ値を検査し、古い権限が残らないようにする。

## 主要データ

- `casts`: 在籍・体入・退店キャスト、月度時給、紹介者、顧問料
- `staff`: 在籍・体入・退店スタッフ、月度時給
- `drivers`: 在籍・退店ドライバー、日給
- `introducers`: 紹介者と報酬条件
- `liquorCosts`: シャンパン・ワイン／キープボトル原価
- `config/cashFloat`: つり銭（初期値200,000円）
- `history`: POS原本、店舗入力、計算結果、承認履歴を含む日次スナップショット
- `history/{id}/submittedAtMs`: Firebaseサーバーが確定した店舗送信順
- `posSubmissionClaims`: POSチェックサムを安全な64桁hexキーとして使い、同じPOS JSONの重複登録を防ぐ所有情報
- `dailyClosingDeletionLock`: 未承認日次の削除と月次確定・状態更新を排他する一時ロック
- `introducerEntryEvents`: 入店顧問料を採用月に一度だけ計上する履歴
- `introducerMonthEvents`: 紹介者削除・同月再設定を人物単位で保持する月次履歴
- `introducerDeletionCommits`: 紹介者削除と対象キャストを証明する変更不能の履歴
- `introducerDeletionLocks`: 紹介者削除中のキャスト変更を防ぐ一時ロック
- `accountingAdjustments`: 月次の源泉所得税・各種手当・固定経費

マスターを完全削除しても `history` 内の名称・時給・原価・金額のスナップショットは変更しない。
送迎ドライバーの日払いは `history` の日次勤務へ保存し、当日現金残額から控除する。
経理側では月次の総支給額から日払い合計を控除して差引支給額を算出する。
経理側のキャスト売上画面は、承認済み `history` をキャスト・出勤日単位に展開し、
本指名／場内延長の売上と酒代原価、指名・同伴本数、各種バック、ボトル、美容室手当を表示する。
キャスト売上の「全員分をXLSX出力」は、選択月の全員をキャスト別シートへ出力する。
月次確定済みの月は保存された売上・報酬スナップショットを使い、現行マスターから再計算しない。
添付キャストデータ表に合わせ、G/J/M列は本指名/場内指名/同伴バック、
P/Q列は通常ボトル/シャンパン・ワインバック、U列はドリンクバックとする。
通常ボトル・シャンパン等の種類別バックは日別・月合計の `bottleBackByKind` に保存する。
旧確定データで内訳が未保存の場合はP/Q列を結合し、保存済みの合計額であることを明記する。
下部には時給＋バックと売上報酬の両方を比較表示し、採用方式と実際の差引支給額を別記する。
売上報酬率は保存された実際の率を表示し、日払い・その他は日払い＋立替＋送迎代の合計とする。
店舗側のキャスト商品明細は、ドリンクをPOS販売単価ごとにまとめて杯数を表示する。
対象外を含むPOS全件商品表は店舗画面には重複表示せず、経理承認画面で確認できる。

キャスト別の本指名売上・場内延長売上、各バック、売上報酬基礎、
売上報酬は10円未満を切り捨てる。キャスト・スタッフの時給は営業日ごとに
1円未満を切り捨てる。ボトル・ドリンクの％バックは商品1行全体の金額を
10円未満切捨て後、対象人数で均等割りし、各人の金額も10円未満切り捨てる
（1,000円を3人で分配する場合は各330円、合計990円。35,000円・原価12,500円の
シャンパンを2人で分配する場合は、商品全体5,620円、各2,810円）。美容室手当、日払い・立替・
送迎代、源泉所得税および差引支給額は追加で丸めず1円単位を保持する。
POSの現金売上・カード売上・当日合計売上は照合対象の実額であり、丸めない。

## 日次状態

`submitted`（店舗送信）→ `approved`（経理承認）または `returned`（差戻し）。
`approved` からも `returned` へ戻せる。差戻し時は実行者・直前状態・理由を記録し、
店舗で再編集・再送してから再承認する。`returned` の期間は月次集計から除外する。
店舗は承認前または差戻し後のデータを `withdrawn` にして再編集できる。
`submitted`・`returned`・`withdrawn` は店舗またはOPが完全削除できる。`approved` は直接削除できず、
経理またはOPによる差戻しが先に必要となる。削除時は `history` 本体、対応する
`posSubmissionClaims`（旧形式キーも含む）、削除ロックを一括更新し、復元不能な物理削除とする。
対象月が月次確定中または確定済みの場合は削除できない。

紹介者報酬は営業日順ではなく、月内で最後に店舗保存された日次の紹介者条件
（紹介者なしを含む）を同月の全日次へ遡って適用する。新規保存は
`submittedAtMs` をFirebaseサーバー時刻で記録する。旧データの保存順を復元できない場合、
または同一サーバー時刻に異なる条件が競合する場合は、推測せず月次確定を停止する。
紹介者削除・同月再設定は日次とは別の原子的な月次履歴で管理し、削除月は報酬なし、
同月に再設定した場合は再設定後の条件を月全体へ適用する。入店顧問料の履歴は
後月のマスター変更では書き換えず、採用日そのものを変更した場合だけ未確定月内で補正する。

Realtime Databaseが空配列を省略して保存するため、日次・POS原本・月次調整は
読込境界で配列と必須オブジェクトを正規化する。構造欠損は画面上で警告する。

## 経費表XLSX

経費画面から、選択月の承認済み日次と保存済み経理入力を `GENESIS経費表_YYYY-MM.xlsx` へ出力する。
見本の列配置・罫線・游ゴシック・A4横向き印刷を使用し、同日同科目の複数支払先は改行で併記する。
各名目欄は支払先だけを表示し、金額は隣の金額欄に集計する。
人員や固定費が見本の行数を超える場合は必要な行を追加して全件を保持する。
未承認・差戻し中・店舗編集中の日次は含めない。

- 広告宣伝①：紹介料。広告宣伝②：広告等。
- 交通費：交通費・その他（交通費に関連するその他を含む）。その他：派遣手数料。
- 美容室：体入キャストの当日支払い分である `beautyTrial` 経費のみ。
- 派遣支払：派遣キャスト支払と派遣スタッフ支払の合計。派遣手数料は重複させない。
- 女子総支給額：採用された報酬方式ごとに、美容室手当を含む控除前の総支給額を集計する。
- スカウト報酬：紹介者IDごとに合計し、紹介者名と合計金額を表示する。キャスト別には展開しない。
- 酒代納品書分：月締め調整後の金額を固定費へ一度だけ計上する。

月次確定後は報酬・給与・紹介者支払に保存済みの確定結果を使用する。
旧確定結果で紹介者IDがない場合は、保存済みの既知IDと一致する情報から復元できる場合だけ集計し、
紹介者を特定できない場合は推測せず出力を止める。
日次のID・チェックサム・更新日時と経理入力の保存世代が確定時の参照情報と一致しない場合は、
推測して補完せず出力を止める。明細・科目別月計・給与等の総支給額・総支出を照合し、
帳票の総支出が経理の収支データと一致することを検証する。
